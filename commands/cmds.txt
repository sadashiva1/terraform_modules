terraform init
terraform init -reconfigure
terraform fmt

terraform plan
terraform apply
terraform apply -auto-approve
terraform plan -refresh-only
--Note: Objects have changed outside of Terraform

------------------------------------dangerous
terraform state rm -dry-run 'github_repository.terraform_repo[0]'
Would remove github_repository.terraform_repo[0]
terraform apply -refresh-only
-----------------------------------------dangerous

terraform show
terraform show -json | jq

terraform plan -out=plan2
terraform applly plan2

this creates a plan3 file :
terraform plan -out=plan3
terraform applly plan3

terraform plan -destroy
terraform destroy

=====================x==================

terraform state list -state=../tfstatefiles/terraform.tfstate
or
terraform state list
>>>>>>>github_branch.test
>>>>>>>github_repository.terraform_repo
>>>>>>>github_repository_file.readme

terraform state show github_repository.terraform_repo

terraform console
terraform console -state=../tfstatefiles/terraform.tfstate
 
terraform output
terraform output -json reponames
terraform output -json
terraform output -state="../tfstatefiles/terraform.tfstate"

==========================x==========================
export TF_VAR_varsource="envar"

terraform plan -var 'varsource=cli'
Changes to Outputs:
  ~ varsource  = "prod.auto.tfvars" -> "cli"

prod.auto.tfvars -> dev.auto.tfvars -> variables.tfvars -> envar
variable not defined - it will ask for input in cli

> contains(["dev","text","staging","prod"],"prod")
true
> contains(["dev","text","staging","prod"],"xyz")
false
>

===========================x=======================
unset GITHUB_TOKEN && gh auth login -h github.com -p https -s delete_repo -w
gh repo create
gh repo delete
gh repo list
gh repo view xyz-repo --web